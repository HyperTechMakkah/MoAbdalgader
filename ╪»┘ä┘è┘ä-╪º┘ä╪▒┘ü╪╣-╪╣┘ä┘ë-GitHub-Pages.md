# رفع الموقع على GitHub Pages مجاناً

النتيجة: موقعك يعمل على `https://USERNAME.github.io` مع شهادة SSL مجانية، بلا
تكلفة وبلا حد زمني، وجاهز لربط دومينك الخاص لاحقاً بخطوة واحدة.

---

## أولاً: اختر شكل المستودع

| الخيار | العنوان الناتج | التوصية |
|---|---|---|
| **مستودع باسم `USERNAME.github.io`** | `https://USERNAME.github.io` | ✅ اختر هذا |
| مستودع بأي اسم آخر | `https://USERNAME.github.io/REPO/` | يعمل، لكن العنوان أطول |

اختر الأول: العنوان أنظف لعلامتك الشخصية، وربط الدومين لاحقاً أبسط، وصفحة الخطأ
٤٠٤ تعمل بشكل صحيح. استبدل `USERNAME` باسم مستخدمك على GitHub — مثلاً
`mohamedabdalgader.github.io`.

---

## ١. جهّز الملفات

في مجلد المشروع على جهازك:

```bash
bash scripts/set-site-url.sh https://USERNAME.github.io
```

ثم فعّل نموذج التواصل: سجّل في <https://web3forms.com> ببريدك (مجاني بلا بطاقة)،
وانسخ `Access Key`، ثم:

```bash
cd static-site
grep -rl WEB3FORMS_ACCESS_KEY . | xargs sed -i 's/WEB3FORMS_ACCESS_KEY/مفتاحك/g'
cd ..
```

---

## ٢. أنشئ المستودع وارفع الملفات

### الطريقة الأولى: من المتصفح (بلا أوامر)

1. افتح <https://github.com/new>
2. اسم المستودع: `USERNAME.github.io` بالضبط
3. اختر **Public** — ضروري لأن Pages المجاني لا يعمل من مستودع خاص
4. **لا** تضع علامة على "Add a README file"
5. **Create repository**
6. في الصفحة التالية اضغط **uploading an existing file**
7. افتح مجلد `static-site` على جهازك، واسحب **محتوياته** كلها (لا المجلد نفسه)
8. اكتب رسالة مثل `Initial site` ثم **Commit changes**

> **مهم:** ملف `.nojekyll` يبدأ بنقطة وقد يخفيه نظامك. على ويندوز فعّل
> "العناصر المخفية" من تبويب View، وعلى ماك اضغط `Cmd + Shift + .`
> بدونه قد لا تظهر بعض الملفات على الموقع.

### الطريقة الثانية: بالأوامر

```bash
cd static-site
git init
git add -A
git commit -m "Initial site"
git branch -M main
git remote add origin https://github.com/USERNAME/USERNAME.github.io.git
git push -u origin main
```

---

## ٣. فعّل GitHub Pages

في المستودع: **Settings** → **Pages** (في القائمة اليمنى)

- Source: **Deploy from a branch**
- Branch: **main** والمجلد **/ (root)**
- **Save**

انتظر من دقيقة إلى ٣ دقائق، ثم حدّث الصفحة — سيظهر رابط موقعك في الأعلى.
افتحه وتأكد أن الصفحة العربية والإنجليزية تعملان.

---

## ٤. اختبر قبل أن تشارك الرابط

- [ ] `https://USERNAME.github.io` يفتح ويحوّل إلى `/en/` أو `/ar/`
- [ ] زر تبديل اللغة ينقلك بين النسختين ويحافظ على الصفحة نفسها
- [ ] صفحات الخدمات تفتح وأسعارها صحيحة
- [ ] القائمة على الجوال تفتح وتغلق
- [ ] أرسل رسالة تجريبية من نموذج التواصل وتأكد أنها وصلت بريدك
- [ ] افتح الموقع على جوالك — لا يجب أن يوجد تمرير أفقي

---

## ٥. عند تعديل المحتوى لاحقاً

**من المتصفح مباشرة:** افتح الملف في GitHub، اضغط أيقونة القلم، عدّل، ثم
**Commit changes**. الموقع يتحدّث خلال دقيقة.

**بالأوامر:**

```bash
git add -A && git commit -m "تحديث الأسعار" && git push
```

**أو اطلب مني:** أرسل لي ما تريد تغييره وسأعطيك الملفات المعدّلة جاهزة للرفع.
كلما كان طلبك محدداً كان أسرع — مثال: «غيّر سعر MVP Blueprint إلى ٣٩٩ دولاراً
في اللغتين» أفضل من «عدّل الأسعار».

---

## ٦. عند شراء الدومين

1. حدّث الروابط الداخلية:

```bash
bash scripts/set-site-url.sh https://mohamedabdalgader.com
```

2. أنشئ ملفاً اسمه `CNAME` (بلا امتداد) في جذر المستودع، محتواه سطر واحد:

```
mohamedabdalgader.com
```

3. عند مزوّد الدومين أضف هذه السجلات:

| النوع | الاسم | القيمة |
|---|---|---|
| A | @ | 185.199.108.153 |
| A | @ | 185.199.109.153 |
| A | @ | 185.199.110.153 |
| A | @ | 185.199.111.153 |
| CNAME | www | USERNAME.github.io |

4. في **Settings → Pages** اكتب الدومين في **Custom domain**، وبعد أن يتحقق منه
   فعّل **Enforce HTTPS**. قد يستغرق إصدار الشهادة حتى ٢٤ ساعة.

---

## ما تخسره في النسخة الثابتة

كن على علم بالفرق قبل أن تعتمد عليها طويلاً:

- **لا لوحة تحكم.** التعديل يتم على ملفات HTML مباشرة أو عبري.
- **المدونة لا تُدار ذاتياً.** إضافة مقال جديد تعني إنشاء مجلد وصفحة يدوياً.
- **الطلبات لا تُحفظ في قاعدة بيانات** — تصلك بريداً فقط عبر Web3Forms، ولا يوجد
  سجل بحالتها (تم التواصل / عرض سعر / فاز).
- **لا إحصاءات للزيارات** إلا إذا أضفت Google Analytics.

هذه النسخة ممتازة كواجهة مؤقتة تُطلق بها اليوم وتضعها في LinkedIn، وتنتقل للنسخة
الكاملة عندما يبرر العائد تكلفة الاستضافة.

---

## ملاحظة على شروط GitHub

GitHub Pages يمنع استخدامه لتشغيل متجر إلكتروني أو موقع «موجّه أساساً لتسهيل
المعاملات التجارية». موقعك يعرض خدماتك وأسعارك ولا ينفّذ أي دفع، وهو أقرب إلى
موقع أعمال شخصي — وهذا مقبول. لكن **لا تضف بوابة دفع أو زر شراء مباشر** على
النسخة المستضافة هنا؛ إن أردت ذلك انقل الموقع إلى استضافة مدفوعة أولاً.
